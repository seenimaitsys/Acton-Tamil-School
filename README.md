# Acton Tamil School

Responsive website for Acton Tamil School, built with React and Vite.

## Run locally

1. Install dependencies with `npm install`.
2. Start the development server with `npm run dev`.
3. Open the local URL shown in the terminal.

## Quality checks

- `npm run lint`
- `npm run build`
